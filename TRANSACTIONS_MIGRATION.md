# Transactions Migration — Personal Wealth v2.4.0

รุ่น v2.4.0 แยก “หมวดหมู่” ออกจาก “ชื่อรายการรายจ่าย” เพื่อให้กราฟสัดส่วนรายจ่ายรวมเป็นกลุ่มที่อ่านง่าย โดยเพิ่ม Header ใหม่ต่อท้ายชีต `Transactions` เพียง 1 ช่อง

## สำรองข้อมูลก่อน

1. เปิด Google Sheet ของ Personal Wealth
2. เลือก `File > Make a copy` หรือดาวน์โหลดสำรอง
3. ตรวจว่าแถว 1 ของชีต `Transactions` เป็น Header เดิมครบ A–H

## เพิ่ม Header

ห้ามแทรกคอลัมน์กลางตาราง ให้เพิ่มต่อท้ายเท่านั้น:

| Cell | Header |
|---|---|
| I1 | `item_name` |

หลัง Migration โครงสร้างต้องเป็น:

| คอลัมน์ | Header |
|---|---|
| A | `tx_id` |
| B | `date` |
| C | `type` |
| D | `category` |
| E | `account_from` |
| F | `account_to` |
| G | `amount` |
| H | `note` |
| I | `item_name` |

## จัดข้อมูลรายจ่ายเดิม

- เปลี่ยนคอลัมน์ D `category` เป็นหมวดใหญ่ เช่น `อาหาร`, `เครื่องดื่ม`, `หนังสือ`, `ทำบุญ`, `ของใช้ส่วนตัว`, `ค่าเดินทาง`, `ครอบครัว`, `สุขภาพ`, `อิเล็กทรอนิกส์`, `อื่น ๆ`
- ใส่ชื่อรายการเดิม เช่น ชื่อร้านหรือสิ่งที่ซื้อ ลงคอลัมน์ I `item_name`
- รายการ Income และ Transfer เว้น `item_name` ว่างได้
- Transaction หมวด `บัตรเครดิต` จะไม่ถูกนำไปคำนวณในกราฟสัดส่วนรายจ่าย แต่ยังคงอยู่ในรายการและ Cash Flow ตามเดิม

ตัวอย่าง:

| type | category | amount | item_name |
|---|---:|---:|---|
| Expense | อาหาร | 120 | ข้าวกลางวัน |
| Expense | หนังสือ | 450 | หนังสือการเงิน |
| Expense | บัตรเครดิต | 5,000 | ชำระยอดบัตรเดือนสิงหาคม |

## หมวดมาตรฐานและการเพิ่มหมวดเอง

แอปมีหมวดมาตรฐานให้เลือกทันที แม้ชีต `Categories` ยังว่าง หากต้องการเพิ่มหมวดใหม่ ให้เพิ่มแถวในชีต `Categories` โดยใส่:

- `category_id`: รหัสไม่ซ้ำ เช่น `exp-pet`
- `category_name`: ชื่อหมวด เช่น `สัตว์เลี้ยง`
- `type`: `Expense`
- `note`: เว้นว่างได้

## ตรวจหลัง Deploy

1. เปิดฟอร์มรายจ่าย ตรวจว่าต้องเลือกหมวดก่อนช่องชื่อรายการจึงพิมพ์ได้
2. บันทึกรายจ่ายทดสอบ 1 บาท และตรวจว่า `category` อยู่คอลัมน์ D ส่วน `item_name` อยู่คอลัมน์ I
3. ตรวจยอด Account ลด 1 บาท
4. ลบรายการทดสอบ และตรวจยอด Account กลับเท่าเดิม
5. เปิดกราฟสัดส่วนรายจ่าย ตรวจว่ารวมตามหมวดและไม่แสดง `บัตรเครดิต`

## Rollback

Header `item_name` สามารถคงไว้ได้หากย้อน Code เป็น v2.3.0 เพราะ Code เดิมจะไม่ใช้คอลัมน์นี้ การย้อน Code ไม่ย้อนยอด Account หรือข้อมูล Transaction ที่บันทึกไปแล้ว จึงควร Reconcile ยอดบัญชีทุกครั้ง
